if(this.disabled) return;
// event.stopImmediatePropagation();
// if(event.target == this.expander || this.expander.contains(event.target))
// this.expander.updateColorTween( new feffects.Tween(event.buttonDown ? -50 : 50, 0, 300, feffects.easing.Expo.easeOut ) );
this.redraw();