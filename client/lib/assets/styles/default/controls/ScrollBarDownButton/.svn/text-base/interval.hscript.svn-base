// parent.handle.updatePositionTween(
// new feffects.Tween( 0, 1, 200, feffects.easing.Linear.easeNone ),
// new flash.geom.Point(0, parent.adjustment.object.step), function(v) {
// 	if(parent.handle.y>=(parent.frame.height - parent.handle.height + 20)) {
// 		this.stopInterval();
// 		parent.handle.updatePositionTween();
// 		parent.handle.y = parent.frame.height - parent.handle.height + 20;
// 	}

// 	parent.adjust();
// });


parent.handle.y += parent.adjustment.getStep();
parent.adjust();