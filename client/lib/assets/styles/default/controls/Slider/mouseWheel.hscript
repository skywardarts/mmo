if(this.disabled) return;
this.adjustment.setValue( this.adjustment.getValue() + event.delta * this.adjustment.object.step * ((this.rotation > 0) ? -1 : 1) );
if(event.ctrlKey)
this.adjustment.setValue( this.adjustment.getValue() + event.delta * this.adjustment.object.page * ((this.rotation > 0) ? -1 : 1) );
